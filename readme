Configure Your Mac to Use the Local DNS Server
Adjust your Mac's DNS settings to use the new DNS server:

Open System Preferences.
Click Network.
Select your network interface (e.g., Wi-Fi).
Click Advanced and then the DNS tab.
Click + to add 127.0.0.1 as your DNS server.

